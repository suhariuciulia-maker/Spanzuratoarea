<Window x:Class="Spanzuratoarea_SuhariucIulia.Views.StatisticsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Statistics" Height="350" Width="500">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Rows}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="User" Binding="{Binding UserName}" Width="*" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*" />
                <DataGridTextColumn Header="Played" Binding="{Binding GamesPlayed}" Width="*" />
                <DataGridTextColumn Header="Won" Binding="{Binding GamesWon}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>